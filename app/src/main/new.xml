<Button android:id="@+id/buttonOpenCamera" android:layout_width="wrap_content" android:layout_height="wrap_content"
    android:background="@drawable/circle_button_background" android:text="Open Camera"
    android:textColor="@android:color/white" android:textAllCaps="false" android:padding="0dp"
    android:minWidth="0dp" android:minHeight="0dp" android:elevation="4dp" />